{
    "version": "0.2.0",
    "configurations": [{
        
        "name": "C/C++Debug",
        "type": "lldb-mi",
        "request": "launch",
        "target": "${file}.o",
        "cwd": "${workspaceRoot}",
        "preLaunchTask": "clang++"
    }]
}